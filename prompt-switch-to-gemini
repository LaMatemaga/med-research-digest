I need to migrate this project from the Anthropic SDK to Google AI Studio (Gemini API).

Context:
- This is a multi-agent medical research digest pipeline
- All agents currently use anthropic.Anthropic() client and client.messages.create()
- Model config is centralized in config.py

Migration targets:
- Use google-generativeai SDK (AI Studio, not Vertex AI)
- MODEL_FAST → gemini-2.0-flash
- MODEL_SMART → gemini-2.5-pro
- Auth: GOOGLE_API_KEY in .env

Tasks:
1. Update requirements.txt — replace anthropic with google-generativeai
2. Update config.py — new model IDs, replace ANTHROPIC_API_KEY with GOOGLE_API_KEY
3. Update .env.example — replace ANTHROPIC_API_KEY with GOOGLE_API_KEY
4. Refactor all agent files — replace Anthropic client pattern with genai.GenerativeModel
5. Handle system prompts — in Gemini they go in system_instruction at model instantiation
6. Update README — replace Anthropic/Claude references with Google AI Studio / Gemini, add link to aistudio.google.com for API key

Keep the pipeline structure, async patterns, and voices layer intact.
After migrating, do a --dry-run to confirm the project loads without errors.
